<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Permissions ===== -->
    <!-- Poloha (fine + coarse) pro GPS tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground tracking + konkrétně polohový typ (API 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- (obecné FOREGROUND_SERVICE permission můžeš nechat, ale od API 34 se bere typově) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Internet pro mapy / API volání -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifikace (vyžadováno od Android 13) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ======================== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorseRideTracker">

        <!-- ==== Aktivity ==== -->
        <activity android:name=".RideActivity"       android:exported="false" />
        <activity android:name=".CalibrationActivity" android:exported="false" />
        <activity android:name=".AddHorseActivity"    android:exported="false" />
        <activity android:name=".HorseSelectActivity" android:exported="false" />
        <activity android:name=".ManageHorsesActivity" android:exported="false" />

        <!-- Hlavní vstupní bod aplikace -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HorseRideTracker">

            <intent-filter>
                <action   android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==== Foreground Service (GPS tracking) ==== -->
        <!-- Pokud ještě nemáš implementaci, ten blok klidně smaž nebo zakomentuj -->
        <service
            android:name=".RideForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- FileProvider (pro sdílení exportovaných souborů, fotek atd.) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
</manifest>
